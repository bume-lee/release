# webpack, babel, react release test

```
npm i -D webpack webpack-cli webpack-dev-server
npm i -D webpack webpack-cli webpack-dev-server
npm i -D babel-loader html-loader
npm i -D webpack-dev-server
npm i react react-dom
```

.babelrc

```
{
  "presets": [
    "@babel/preset-env",
    "@babel/preset-react"
  ]
}
```
